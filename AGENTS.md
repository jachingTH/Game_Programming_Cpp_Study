# 학습 저장소 작업 규칙

## 목적과 범위
- 이 저장소는 게임 프로그래밍 C++ 학습용이다. 설명·기록은 한국어, 코드 식별자는 명확한 영어를 기본으로 한다.
- README와 ROADMAP을 먼저 읽고 현재 주제와 사용자의 요청 범위를 확인한다.
- 사용자가 직접 구현한 내용과 기존 기록을 보존한다. 관련 없는 파일을 수정하지 않는다.

## 학습 지원
- 리뷰 요청에서는 문제·원인·재현 조건·힌트를 먼저 설명한다.
- 수정 요청이 있으면 요청 범위의 수정을 수행한다. 이미 요청된 수정에 재승인을 요구하지 않는다.
- 사용자에게서 제공되지 않은 이해도, 경험, 학습 완료 상태, 면접 답변을 사실처럼 작성하지 않는다. 답변 예시는 예시로 표시한다.
- 가능한 가장 단순한 구현을 선택하고 필요 없는 의존성이나 추상화를 추가하지 않는다.

## 코드와 검증
- 컴파일러, C++ 표준, 빌드 방법은 기존 프로젝트 설정을 따른다. 설정이 없으면 작업에 맞는 선택을 기록한다.
- 객체 수명, 소유권, 복사·이동, 경계 조건을 확인한다. 직접 메모리 관리 학습을 제외하면 RAII를 우선한다.
- 변경의 위험에 맞는 의미 있는 검증을 수행한다. 문서 편집만으로 불필요한 테스트를 추가하지 않는다.
- 실행하지 않은 테스트를 통과했다고 쓰지 않는다. 성능 수치는 측정 환경과 조건을 함께 기록한다.
- Unreal 실습은 엔진의 객체 수명과 빌드 체계를 따른다.

## 기록과 저장
- 구현 설명·실행 방법은 해당 주제 README, 날짜별 결과는 STUDY_LOG에 기록한다.
- ROADMAP은 완료 근거가 있을 때만 체크한다.
- Obsidian 경로: D:\Obsidian\Obsidian Vault\Learning_Programming
- Obsidian 작성 요청 시 Templates의 양식을 사용해 개념·질문·복습을 정리하고 코드 경로를 연결한다.
- `.obsidian` 설정, 기존 노트, Git 이력을 임의로 덮어쓰거나 삭제하지 않는다.
- 커밋·원격 업로드는 사용자가 요청한 범위에서 수행한다. 비밀키, 토큰, 개인정보를 기록하지 않는다.